import { EnterKeySubmitDirective } from './enter-key-submit.directive';

describe('EnterKeySubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterKeySubmitDirective();
    expect(directive).toBeTruthy();
  });
});
