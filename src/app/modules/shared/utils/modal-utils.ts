import { NgbModalUpdatableOptions } from "@ng-bootstrap/ng-bootstrap";

/** Aplica a classe .hidden-modal no modal. */
export const hiddenModal: NgbModalUpdatableOptions = {
  windowClass: 'hidden-modal'
};

/** Aplica a classe .visible-modal no modal. */
export const visibleModal: NgbModalUpdatableOptions = {
  windowClass: 'visible-modal'
};
